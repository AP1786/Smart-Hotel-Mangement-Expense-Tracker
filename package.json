{
  "name": "smart-hotel-hybrid-dapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "contracts",
    "shared"
  ],
  "scripts": {
    "audit:slither": "npm run audit:slither -w contracts",
    "build": "npm run build -w shared && npm run build -w contracts && npm run build -w backend && npm run build -w frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint backend/src shared/src frontend/src contracts/hardhat.config.js contracts/scripts contracts/test",
    "prepare": "node .husky/install.mjs",
    "quality:check": "npm run format:check && npm run lint && npm run test",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "globals": "^16.0.0",
    "husky": "^9.1.7",
    "prettier": "^3.5.3"
  }
}
